package com.ck.reusable.springboot.domain.History;

import org.springframework.data.jpa.repository.JpaRepository;

public interface returnHistoryRepository extends JpaRepository<return_history, Long> {
}
